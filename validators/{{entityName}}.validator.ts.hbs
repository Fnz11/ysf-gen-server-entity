import { z } from "zod";

// ==================== VALIDATION ====================
// Create {{ entityNameCap }} Schema Validator
{{{ zodValidator }}}

// ==================== TYPES ====================

export type Mutate{{entityNameCap}}SchemaProps = z.infer<typeof mutate{{ entityNameCap }}Schema>;
export type getAll{{entityNameCap}}Props = {
    offset?: string | null;
    limit?: string | null; 
    {{idName}}_or?: string[] | null; {{#each fieldsFilter}}
    {{#if (eq type "unknown")}}{{else}}
    {{key}}?: string | null;{{/if}}
    {{/each}} 
  }